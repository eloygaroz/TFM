<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                              http://maven.apache.org/xsd/settings-1.0.0.xsd">
    <localRepository>${user.home}/.m2/repository</localRepository>
    <interactiveMode>true</interactiveMode>
    <usePluginRegistry>false</usePluginRegistry>
    <offline>false</offline>
    <pluginGroups>
        <pluginGroup>org.apache.maven.plugins</pluginGroup>
    </pluginGroups>
    <servers>
        <!-- Example: Define servers for repository authentication -->
        <!--
        <server>
            <id>repo.example.com</id>
            <username>your_username</username>
            <password>your_password</password>
        </server>
        -->
    </servers>
    <mirrors>
        <!-- Example: Define mirror for repository -->
        <!--
        <mirror>
            <id>mirror.example.com</id>
            <mirrorOf>*</mirrorOf>
            <url>http://repo.example.com/maven2</url>
            <blocked>false</blocked>
            <mirrorOfLayouts>default,legacy</mirrorOfLayouts>
        </mirror>
        -->
    </mirrors>
    <proxies>
        <!-- Example: Define proxies for network connections -->
        <!--
        <proxy>
            <id>example-proxy</id>
            <active>true</active>
            <protocol>http</protocol>
            <host>proxy.example.com</host>
            <port>8080</port>
            <username>your_proxy_username</username>
            <password>your_proxy_password</password>
            <nonProxyHosts>www.google.com|*.example.com</nonProxyHosts>
        </proxy>
        -->
    </proxies>
    <profiles>
        <!-- Example: Define profiles for various environments -->
        <!--
        <profile>
            <id>development</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <env>development</env>
            </properties>
        </profile>
        <profile>
            <id>production</id>
            <properties>
                <env>production</env>
            </properties>
        </profile>
        -->
    </profiles>
    <activeProfiles>
        <!-- Example: Activate profiles based on environment -->
        <!--
        <activeProfile>development</activeProfile>
        -->
    </activeProfiles>
</settings>
